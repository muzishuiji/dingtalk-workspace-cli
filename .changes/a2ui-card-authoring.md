---
category: Added
---

- **A2UI card authoring** — adds `dws card` protocol discovery, recipes, blocks, composition, validation, sanitized Markdown-aware local reference previews, and profile-scoped stateful send/update/finish workflows. Local previews are structural references, not DingTalk client fidelity evidence.
